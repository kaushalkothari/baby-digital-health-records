/** From `.env` — keep in sync with `VITE_APP_TITLE` / `VITE_APP_DESCRIPTION` (also used in `index.html`). */
export const APP_TITLE = import.meta.env.VITE_APP_TITLE ?? "BabyBloom";
export const APP_TAGLINE = import.meta.env.VITE_APP_DESCRIPTION ?? "Nurturing every little milestone";
